---
description: View or edit the business rules Red Pill must never break
---

Read `.redpill/policies.json` (if absent, say no rules are set and offer the seeded list from
`/redpill:setup` question 4). Show current rules as a numbered list. Let the user add/remove
via bounded questions (numbered choices baseline; cards where supported). Save only what they
confirm. Remind them: rules are applied as hard constraints on future runs and every
application is disclosed in the report.
